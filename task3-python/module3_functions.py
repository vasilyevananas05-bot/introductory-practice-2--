Типы данных:
int (integer) — целые числа (например, 5, -10, 1000).
float (floating point) — дробные числа, где десятичная часть отделяется точкой (например, 3.14, 0.5).
str (string) — строки, любой текст внутри кавычек.
bool (boolean) — логический тип, имеющий только два значения: True (истина) или False (ложь).
Операторы — это специальные символы для вычислений: + (сложение), - (вычитание), * (умножение), / (деление).
print() — функция, которая выводит информацию на экран.
input() — функция, которая позволяет пользователю ввести данные с клавиатуры. Важно помнить, что input всегда принимает данные как текст (str), поэтому для цифр его нужно оборачивать в int() или float().
